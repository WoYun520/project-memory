# 虚构练习项目：离线计时器

这个目录只含演示页面，不含真实项目历史。可以把本文件夹连接到记忆站练习交接。

第一次给 AI 的任务：阅读本目录，说明已有行为，再加一个暂停按钮，保留开始和重置。完成后提交有依据的草稿。

检查保存后换一个 AI：接着给计时器增加键盘操作，并说明哪些内容是刚从记忆读取、哪些是本次检查。不要编造历史失败尝试。

打开 index.html 即可运行，无外部依赖。此任务文案仅是练习建议，不会自动执行。
